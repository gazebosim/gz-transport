#!/bin/sh

export IGN_CONFIG_PATH=@IGN_CONFIG_PATH@

echo 'not empty file' > ign_log_record_force.tlog

ign log record --file ign_log_record_force.tlog --force &
record_pid=$!
sleep 2
kill -9 $record_pid
ret=$?

rm ign_log_record_force.tlog
return $ret
