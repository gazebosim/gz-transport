#!/bin/sh

export IGN_CONFIG_PATH=@IGN_CONFIG_PATH@

echo 'not empty file' > ign_log_record_no_overwrite.tlog

ign log record --file ign_log_record_no_overwrite.tlog
ret=$?

rm ign_log_record_no_overwrite.tlog
return $ret
